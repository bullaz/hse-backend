subtilite a finir :

- button dessous mesure controle
- delete measure controle
- change icon autre risk hazard
- invalide text explaining what was wrong
- those header padding problems bruh


-verify if mobile is production ready

**** remember: there is potential memory leak in that authProvider (about mount)
authProvider(potential)

front: 

-better handling of errors (both backend and frontend) (login for example, don't just throw errors without handling it ) (backend: choose carefully the error codes to send)
-




add: description field to toko5: (little description of contractant's job)



I think portal component should only wrap modal in your project ;

The <CameraView> component does not support children. This may lead to inconsistent behaviour or crashes. If you want to render content on top of the Camera, consider using absolute positioning.






network detection : 

import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import NetInfo from '@react-native-community/netinfo';

const NetworkStatusComponent = () => {
  const [isConnected, setIsConnected] = useState(true);

  useEffect(() => {
    const unsubscribe = NetInfo.addEventListener((state) => {
      setIsConnected(state.isConnected);
      // state.isInternetReachable provides a more reliable check for actual internet access
    });

    // Unsubscribe from events when component unmounts
    return () => {
      unsubscribe();
    };
  }, []);

  return (
    <View style={styles.container}>
      <Text style={styles.statusText}>
        {isConnected ? '✅ Online' : '❌ Offline'}
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  statusText: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default NetworkStatusComponent;


export interface Toko5Json {
  toko5_id: string,
  nom_contractant: string,
  prenom_contractant: string,
  date_heure: string,
  etat: string,
  listMesureControle: [],
  listCommentaire: [],
  listProblem: []
}

toko5Json should be camelCase

possible : no internet during the creating of toko5 .. the invalide (there is internet) (so update doesn't work.. should verify and use add or update)



!!!!!!!!!!!!!!!!!!!!!!!
→ Caused by: database is locked]
in mobile.. VERIFY WHAT CAUSED THAT




Deprecated feature used
Unload event listeners are deprecated and will be removed.

1 source
sockjs-client.js?v=c8f69ccd:66



don't forget
mesure controle component:
WARN: currently we support only numbers in height prop


don't forget alert for errors;

don't forget to throw exceptions and handle them properly



*******************if i login and invalidate toko 5 fast the server only get the create toko 5 not the second call (add list reponse, etat invalide)
important************** .. maybe it is because while spring boot process the create toko5, the add list reponse is already processed also so... maybe wait for the first to finish before the second (axios instance)



don't forget to handle permission better scanqr code (use dialog)







remarques: 

intervenant 

identification:
num phone
mail

tache

ajouter nom, description etape(with that lamp icon)

ajout poste superviseur

risk acceptance: (notification vers hse)
-attenuer
-accepter (responsabilite)
-resolu

interface statistique
-

preuves ---- toko5

basse de donnees: backup




change url toko5s/toko5 to toko5s/








tasks today:  
front:
-crud societe
-crud task + list required questions with it
(update logic with it: check validity, check required questions)
-show list problem(front)
-create a template stats interface / with mock list stats

add admin interface for adding:
-crud supervisor with mail verification
-crud hse with mail verification


mobile:
-login: add task
-dropdown societe
-add num phone, email
-second chance toko 5
-description chaque etape en dessus list pictograms (with that lamp icon)
-try add name picto below each checkbox and see if it looks good
-login supervisor: add poste
-add language/ choose language at the home component en dessous




task crud: 
-don't forget to configure for dark mode


...:
before executing any task/work, after all types of process workers need to complete, workers also need to complete a paper called toko5.. It is a paper by workers to self verify the safety before executing any task : safety for himself and his surroundings. If there are problems the worker is not allowed to start his task for his safety and the surrounding safety and the company safety..  Here are the pics of the toko5.. 
It is a list of checkboxes the worker should complete(for his current state, his materials, epi, ...) and a list of checkboxes for potential risk and hazards he encounters at his place of work.. and for all risk he check he gotta write control measure for it and say if it is implemented or not.
basically that.. The worker can review and change his toko5 at any time
My task was to digitize that paper.
When the worker chose his task in the toko5 mobile (each task have the list of imperative checkboxes the worker shoudl check to validate his toko5.. If not the toko5 is invalid (meaning there are problems) (those checkboxes are the necessary checkboxes for that specific taks))
It includes :

1- a mobile interface for the workers to complete their toko5s.

2- a mobile interface for supervisor for this scenario: - if any toko5 is invalid, the worker must go to his supervisor the supervisor by his interface can scan to toko5 to see all imformations about it.. He can comment it at anytime to say things about the worker's work, or anything,.. and can revalidate the toko5 so the worker can continue his toko5 if the problem is resolve(for example the worker didn't check the glove epi which is necessary so he goes to the supervisor, the supervisor gives him gloves and then revalidate the toko5)

3-web interface for the responsible at the company (pas sur terrain) where: - they can see in real time the state of toko5s of workers (was is valid, was there problems(what was the problem)) (and get notified right away if some worker have invalid toko5 (have some problems)) (basically everything of the toko5s (comments by supervisor,..)), -they can also see stats about toko5s(
percentage of worker with problems(per week/ per month)
top most encountered problems (week, month)
)

I do the project solo doing the fullstack work(database, backend, frontend, mobile) alone

here are screenshot of the project: 

The project is almost finished but I want you to tell me how long do you give an intern (cs master 2 intern)(that project as a part of a final year project) to finish that project (how many weeks are month)



éÉâÂèÈ